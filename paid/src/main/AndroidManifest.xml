<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paid">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ListActivity">
        <activity android:name=".ViewListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:theme="@style/Theme.AboutActivity">
        </activity>
        <activity
            android:name=".AddActivity"
            android:windowSoftInputMode="stateVisible|adjustResize"
            android:theme="@style/Theme.Ass2_MyAddress">
        </activity>
        <provider android:name=".MyBookContentProvider"
            android:authorities="com.example.books" />
    </application>

</manifest>